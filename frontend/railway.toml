[build]
builder = "RAILPACK"
buildCommand = "npm run build"
watchPatterns = [
  "/frontend/**"
]

[deploy]
startCommand = "HOSTNAME=0.0.0.0 npm run start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
